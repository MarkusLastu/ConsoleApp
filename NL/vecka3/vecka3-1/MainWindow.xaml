<Window x:Class="vecka3_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:vecka3_1"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="450" 
        Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Katt -->
        <Label Grid.Row="0" Grid.Column="0" Content="Välj katt:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbCats" Grid.Row="0" Grid.Column="1" DisplayMemberPath="Name" Margin="5"/>

        <!-- Ålder -->
        <Label Grid.Row="1" Grid.Column="0" Content="Välj ålder:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbAge" Grid.Row="1" Grid.Column="1" DisplayMemberPath="Age" Margin="5"/>

        <!-- Katthem -->
        <Label Grid.Row="2" Grid.Column="0" Content="Välj katthem:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbShelters" Grid.Row="2" Grid.Column="1" DisplayMemberPath="City" Margin="5"/>

        <!-- Katt - egenskap och värde -->
        <Label Grid.Row="3" Grid.Column="0" Content="Katt - egenskap:" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <ComboBox x:Name="cmbCatProperty" Width="150" Margin="0,0,10,0" SelectionChanged="cmbCatProperty_SelectionChanged"/>
            <ComboBox x:Name="cmbCatValue" Width="150"/>
        </StackPanel>

        <!-- Djurhem - egenskap och värde -->
        <Label Grid.Row="4" Grid.Column="0" Content="Djurhem - egenskap:" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <ComboBox x:Name="cmbShelterProperty" Width="150" Margin="0,0,10,0" SelectionChanged="cmbShelterProperty_SelectionChanged"/>
            <ComboBox x:Name="cmbShelterValue" Width="150"/>
        </StackPanel>

        <!-- SELECT - uttryck -->
        <Label Grid.Row="5" Grid.Column="0" Content="SELECT:" VerticalAlignment="Top"/>
        <Grid Grid.Row="5" Grid.Column="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtSelect" Grid.Column="0" Height="60" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            <Button x:Name="btnRun" Grid.Column="1" Content="Kör" Margin="10,0,0,0" Height="60" Click="btnRun_Click"/>
        </Grid>

        <!-- Resultat -->
        <Label Grid.Row="6" Grid.Column="0" Content="Resultat:" VerticalAlignment="Top" Margin="0,15,0,0"/>
        <TextBox x:Name="txtResult" Grid.Row="6" Grid.Column="1" Margin="5,15,5,5" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</Window>