<Window x:Class="V3_Dag3_Tutorial_MVVM2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:V3_Dag3_Tutorial_MVVM2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
    
    <StackPanel Margin="5"
                Grid.Column="0"
                Grid.Row="0">
            <Button Content="Ladda katter"
                    Command="{Binding LoadCatsCommand}" />
            
            <ListBox ItemsSource="{Binding Cats}"
                     SelectedItem="{Binding SelectedCat}" 
                     DisplayMemberPath="Name"/>
            </StackPanel>
        <Grid Margin="5"
              Grid.Column="1"
              Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Margin="5"
                       Grid.Column="0"
                       Text="Färger:"/>
            <ComboBox Margin="5"                      
                      Grid.Column="1"
                      Grid.Row="0"
                      ItemsSource="{Binding CatColors}"
                      SelectedItem="{Binding SelectedColor}" />
            <Button Margin="5"
                    Grid.Column="2"
                    Grid.Row="0"
                    Content="Sök"
                    Command="{Binding LoadCatsByColorCommand}"/>

            <TextBlock Margin="5"
                       Grid.Column="0"
                       Grid.Row="1"
                       Text="Shelter:"/>
            <ComboBox Margin="5"
                      Grid.Column="1"
                      Grid.Row="1"
                      ItemsSource="{Binding Shelters}"                      
                      SelectedItem="{Binding SelectedShelter}"
                      DisplayMemberPath="Name"/>
            <Button Margin="5"
                    Grid.Column="2"
                    Grid.Row="1"
                    Content="Sök"
                    Command="{Binding LoadCatsByShelterCommand}"/>

            

        </Grid>



    <StackPanel Margin="5"
                Grid.Column="0"
                Grid.Row="1">
        <TextBlock Text="Resultat:"/>
        <TextBlock Text="{Binding SelectedCat.Id}"/>
        <TextBlock Text="{Binding SelectedCat.Name}"/>
                    <TextBlock Text="{Binding SelectedCat.Age}" />
        <TextBlock Text="{Binding SelectedCat.Color}" />
    </StackPanel>
        

    </Grid>
</Window>
