<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GrupparbeteVecka4.Views"
             x:Class="GrupparbeteVecka4.App">
    <Application.Resources>

        <ResourceDictionary>

            <!-- MAUI:s standardresurser -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Egna färger och bakgrunder -->
            <LinearGradientBrush 
                x:Key="LiveBackground" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFF5F5" Offset="0.0" />
                <GradientStop Color="#FED7D7" Offset="1.0" />
            </LinearGradientBrush>

            <Color x:Key="PrimaryButtonColor">#0F172A</Color>
            <Color x:Key="PrimaryTextColor">#FFFFFF</Color>
            <Color x:Key="HeaderTextColor">#0F172A</Color>
            <Color x:Key="SubHeaderTextColor">#475569</Color>

            <!-- Grundstil för hela appen -->
            <Style TargetType="Grid">
                <Setter Property="RowSpacing" Value="10" />
                <Setter Property="ColumnSpacing" Value="10" />
            </Style>

            <Style TargetType="VerticalStackLayout">
                <Setter Property="Spacing" Value="15" />
                <Setter Property="Padding" Value="20" />
            </Style>
            
            <!-- Labels -->
            <Style x:Key="DefaultLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource SubHeaderTextColor}" />
                <Setter Property="FontSize" Value="16" />
            </Style>
            
            <Style x:Key="HeaderLabelStyle" TargetType="Label"
                   BasedOn="{StaticResource DefaultLabelStyle}">
                <Setter Property="FontSize" Value="32" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource HeaderTextColor}" />
            </Style>

            <Style x:Key="SubHeaderLabelStyle" TargetType="Label"
                   BasedOn="{StaticResource DefaultLabelStyle}">
                <Setter Property="FontSize" Value="18" />
            </Style>

            <!-- Buttons -->
            <Style x:Key="DefaultButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryButtonColor}" />
                <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="HeightRequest" Value="50" />
            </Style>

            <Style x:Key="AnswerButtonStyle" TargetType="Button"
                   BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HeightRequest" Value="55" />                
            </Style>

            <!-- Pickers -->
            <Style x:Key="DefaultPickerStyle" TargetType="Picker">
                <Setter Property="TextColor" Value="{StaticResource HeaderTextColor}" />
                <Setter Property="TitleColor" Value="{StaticResource SubHeaderTextColor}" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="HeightRequest" Value="60" />
                <Setter Property="WidthRequest" Value="300" />
                <Setter Property="BackgroundColor" Value="#FFFFFF" />
            </Style>

            <!-- TextFields -->
            <Style x:Key="DefaultEntryStyle" TargetType="Entry">
                <Setter Property="TextColor" Value="{StaticResource HeaderTextColor}" />
                <Setter Property="PlaceholderColor" Value="{StaticResource SubHeaderTextColor}" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="HeightRequest" Value="60" />
                <Setter Property="BackgroundColor" Value="#FFFFFF" />
            </Style>




        </ResourceDictionary>
    </Application.Resources>
</Application>